'1-->__call__,直接调用方法，instance.method()'

class Student(object):
	def __init__(self,name):
		self.name=name
	
	def __call__(self):
		print('My name is %s' % self.name)
		
a=Student('Jin')
a()

'2-->callable'
callable(max)
